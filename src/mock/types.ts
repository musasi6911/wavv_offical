export interface topSingerType {
  name: string;
  pic: string;
}

export interface songDetailType {
  title: string;
  composer: string;
  pic: string;
}

export interface tracksDetailType {
  title: string;
  clickNum: number;
  duration: string;
  pic: string;
}
