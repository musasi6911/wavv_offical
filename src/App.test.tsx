import { describe, it } from "vitest";
import { render, screen } from "@testing-library/react";
import App from "./App";

describe("App", () => {
  it("Render Hello Vite React", () => {
    //ARRANGE
    render(<App />);
    //ACT
    //EXPECT
  });
});
